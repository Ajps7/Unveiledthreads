# Unveiled Threads - PRD

## Problem Statement
Full-featured UK streetwear marketplace for independent/small-medium brands with social commerce, brand applications, boosted promotions, in-app messaging and shipping. Modern streetwear aesthetic (black / silver / neon green).

## Architecture
- Frontend: React + Tailwind + Shadcn UI (brutalist black/silver/neon green #39FF14)
- Backend: FastAPI + MongoDB (~2700 lines in server.py — candidate for refactor)
- Payments: Stripe (boosts + purchases with **Buyer Protection fee: 5% of subtotal + £0.49, capped £6.00/order** + shipping) — LIVE test keys set (sk_test_51TO33K...)
- Auth: JWT httpOnly cookies
- Storage: Emergent Object Storage
- Emails: Resend (LIVE)
- Shipping: **Brands handle their own shipping** (Vinted/Depop model). They print labels via Royal Mail / Hermes / etc on their own, then enter courier + tracking number into the app via "Mark as Shipped" form. Platform doesn't generate labels.

## Complete Feature List
- JWT auth (register, login, logout, refresh, 401 axios interceptor)
- Brand application → admin approval with Resend email notifications
- Product CRUD with image upload + per-product shipping cost
- Advanced product filters: size, colour, material, fit, gender, condition
- Brand profiles with logo/banner — **NO external links (Instagram/website removed)** to prevent off-app sales
- Brand of the Week (admin-set)
- Boosted Brand promotions (Stripe)
- Buyer purchase flow (Buyer Protection fee 5% + £0.49 capped £6 + shipping) via Stripe
- Order tracking + Vinted-style shipping timeline
- Shipping labels (Shippo when key set, mock otherwise)
- Reviews & Ratings (product + brand)
- Wishlist
- Brand analytics dashboard
- Referrals — **currently hidden as "Coming Soon" placeholder** (user request)
- In-app messaging with strict automated content surveillance (emails / phones / URLs / PayPal / Venmo / WhatsApp blocked)
- Community feed + product comments
- Real-time notification polling (10s interval)
- Terms & Conditions page + acceptance checkboxes at registration/application
- 8+ clothing categories

## Changelog (recent)
- 2026-08: **Four-feature drop — image moderation + change-email + favicon + structured description**.
  **(1) Image moderation on product create/update.** New `moderate_product_images()` in `core.py` runs per-image: always-on server-side gates (magic bytes / Pillow decode / 8MB / 8000px cap) plus a swappable external provider via `IMAGE_MODERATION_PROVIDER` env (default `none`; `claude` uses Claude Haiku vision through the existing Emergent LLM key — no extra key). Verdicts: `passed` / `flagged` / `unverified`. `POST /products` and `PUT /products/{id}` reject any `flagged` image with a 422 that names only the image position ("Image 2 was flagged and can't be used") — internal reason stays in logs. `unverified` (no provider configured OR external image URL OR unclear verdict) is stored but the product's `moderation_status` becomes `needs_review` for admin sweeping. Public `/products` list hides `moderation_status="flagged"`. NEW admin endpoints `GET /admin/moderation/products` + `POST /admin/moderation/products/{id}` (approve/take-down). Per-image record on the product doc: `images_moderation: [{index, url, status, reason}]`. Update path re-scans only newly-added images so re-saving unchanged edits is free.
  **(2) Change email — two-step, mirrors the reset-password token pattern.** NEW `POST /auth/change-email/request` (rate-limit 5/hour, auth required, re-authenticates via `current_password`, rejects same-email 400 and in-use email 409, single in-flight token per user, hashed SHA-256 in `db.email_change_tokens`, 1-hour expiry, single-use). Sends the verification link to the NEW inbox and a security-notice email to the OLD inbox so account-takeover is visible. NEW `POST /auth/change-email/confirm` (10/hour) validates + consumes token, flips `users.email`, wipes any outstanding password-reset tokens, and deletes the `access_token` cookie (forces sign-in with new address → re-secures account). Frontend: NEW **Change email** card on `/account` (data-testid=`change-email-section`) with post-submit "check your new inbox" confirmation. NEW `/verify-email-change?token=…` landing page (`VerifyEmailChange.js`) with loading/OK/error states in the brutalist theme.
  **(3) Favicon + tab title.** Added `favicon.ico`, `favicon.svg`, `favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png` (180×180) in `/app/frontend/public/` — near-black (#050505) background with neon-green (#39FF14) `UT` mark, matching the brand. Wired into `<head>` in `public/index.html`. Default `<title>` set to **Unveiled Threads** (was "Emergent | Fullstack App"); per-page titles that already exist keep the pattern `{Page} · Unveiled Threads`. Meta description + theme-color updated to match. `VerifyEmailChange` page also sets `document.title = "Confirm email · Unveiled Threads"`.
  **(4) Structured product description.** Added 5 optional fields to `ProductCreate` / `ProductUpdate` — all length-capped and validated: `story` (300 chars), `details` (array of ≤8 strings, each ≤120 chars, whitespace stripped, empties dropped), `materials` (120), `fit_notes` (200), `care` (200). Backward-compatible: existing products with only `description` render identically (freeform block still shown). Frontend `ProductDescription` gained: (a) an italicised story paragraph with a neon-green left border at the top when `story` present, (b) a bulleted `details` block with `#39FF14` markers, (c) a grid of labelled spec rows (Materials / Fit / Care) — each section hidden entirely when its field is empty (no dangling labels). AddProduct form has a new **Listing highlights** card between Pricing and Pre-order, with helpful placeholder examples for every field (no forced completion — brands are guided into a good listing without extra work).

  **Testing**: dedicated acceptance run (28 checks: image moderation 6, change email 12, favicon 5, structured description 5) — all 4 acceptance criteria proven end-to-end, including the corrupt-image → 422 flag path with the seller-facing "Image 1 was flagged" message. Full backend suite: **121 passed, 1 skipped, 1 pre-existing flake** (unrelated ordering issue in iter13 email test).

- 2026-08: **Tidier product description block** — long seller-authored copy no longer hits buyers as a wall of text. New `ProductDescription` component in `components/product/ProductDescription.jsx` (~135 lines): (1) blank lines split into real paragraphs; (2) lines starting with `-`, `•`, or `*` promote to a bulleted list with neon-green (#39FF14) markers; (3) descriptions over 280 chars collapse behind a "SHOW MORE / SHOW LESS" toggle so the Buy button stays above the fold; (4) a scannable attribute chip row surfaces the structured product metadata (colour, material, gender, condition, fit) that existed on the model but was previously invisible on the product page — gender/condition are hidden when they equal the defaults ("unisex" / "new") to avoid noise. A `DETAILS` header sits above the block for scannability. Backward-compatible: products with short descriptions and no attributes render unchanged. Verified end-to-end on preview with a 729-char multi-format description (paragraphs + bullet list) — both collapsed and expanded states screenshot cleanly.

- 2026-08: **Pre-Order (Model A: charge now, ship later)** — reuses the existing checkout/settlement/Stripe Connect flow verbatim; this is an extension, not a rebuild. (1) Product model: added `is_preorder`, `preorder_ship_date`, `preorder_limit` to `ProductCreate`/`ProductUpdate`/stored doc — pre-orders REQUIRE a future ship date (422 otherwise); `preorder_limit` optional cap (≥1). Backward-compat: existing products default `is_preorder=false`, behave identically. (2) Checkout: `/api/orders/checkout` allows stock=0 IFF `is_preorder=true`; enforces `preorder_limit` via a count of existing non-refunded pre-orders (409 "Pre-order sold out." if reached); Stripe charge unchanged. (3) Order settlement: new status value `preorder_paid` (distinct from `paid`), set in `settle_paid_order` — everything else runs identically (receipt email, referral credit, brand notification). Stock is NOT decremented and oversell guard is SKIPPED for pre-orders. (4) **Stripe Connect Express** account creation in `_finalise_approval` now sends `settings.payouts.schedule = {"interval": "daily", "delay_days": STANDARD_PAYOUT_DELAY_DAYS (7)}` — rolling 7-day payout buffer applied to EVERY order (buyer-protection safeguard). Reserved `PREORDER_PAYOUT_DELAY_DAYS` constant with an explicit code comment explaining why we do NOT implement per-order timing (would require separate charges & transfers → funds held on platform → regulatory implications). (5) `SALE_STATUSES = ["paid", "preorder_paid", "shipped", "delivered"]` added to `core.py`; applied to analytics (`brand-analytics` current + prior periods), engagement (low-stock digest + abandoned-checkout), disputes stats, and admin cascade guards — pre-order revenue is now visible everywhere in-stock revenue is. (6) Frontend: `PRE-ORDER` badge on ProductCard, "Ships by {date}" line below the name; ProductDetail shows "Pre-order — ships by {date}" (green) instead of stock count and a green banner explaining the money-back guarantee if not shipped by date; buy button relabels to "PRE-ORDER — £X.XX". AddProduct has a new **Sell as pre-order** section with checkbox + future-only date input + optional cap. Buyer's MyOrders and BrandDashboard render distinct "Pre-order — ships by {date}" / "Pre-order — awaiting dispatch (ships by {date})" status pills so pre-orders can NOT be mistaken for overdue in-stock orders. Dispute + review eligibility extended to `preorder_paid` (buyers need the safeguard more, not less). (7) Receipt email adds a pre-order banner via `esc()`-escaped values — non-preorder receipts unchanged. Ship-order route accepts both `paid` and `preorder_paid`. **Testing**: 6 new pre-order tests all green; full suite still 116 passed / 1 skipped / 0 failed.

- 2026-08: **Rate-limiting bypass + message email-flood fix** — (Problem A) `core._client_ip` was resolving XFF as `.split(',')[0].strip()`, which returns the LEFTMOST — attacker-controlled — entry (proxies APPEND; only rightmost hops are trusted). NEW: `TRUSTED_PROXY_HOPS` env var (default 1) + parse XFF into a list and index from the right as `parts[-hops]`, with a fallback to socket IP when the header is too short or absent. Closes the classic bypass where any client could send a rotating `X-Forwarded-For: <random>` to get a fresh limiter bucket per request. Also gated X-Real-IP behind `hops >= 1`. (Problem B) Two-layer message-flood fix: (1) `@limiter.limit("20/minute")` on `POST /messages/send` (generous for real conversation, fatal to a flood); (2) conversation-level `last_email_at` cooldown (env `MESSAGE_EMAIL_COOLDOWN_MINUTES`, default 15) — a burst of 50 messages now produces 50 in-app notifications and exactly ONE email. In-app is never dropped; only the email is suppressed. Adds `send_email` parameter propagation into `create_notification` (already supported). (Problem C) Explicit `@limiter.limit(...)` on `POST /community/posts` (10/min), `POST /community/posts/{id}/comments` (20/min), and `POST /products/{id}/comments` (20/min) — previously only the 120/min global default. (Testing) Since the tests previously relied on `X-Forwarded-For` spoofing to isolate limiter buckets (which is now closed), added a non-prod-only `POST /api/__test/reset-limiter` endpoint and a shared `tests/conftest.py` autouse fixture that resets limiter state before every test. Legacy iter12 test assertions on the old `422 min_length` Pydantic error updated to the new `400 validate_password()` message. Test run: **116 passed, 1 skipped, 0 failed** (~127s), including the new `tests/test_rate_limiting.py` with 7 tests covering all A/B/C acceptance criteria.
- 2026-07: **Founding-Brand email + magic-byte uploads + component refactor** — (P2a) NEW `send_application_received_email(recipient, brand_name, is_waitlisted)` in `core.py` fires from `POST /api/brands/apply` on the pending + waitlisted paths (auto-approved brands already receive the Stripe onboarding email — no double-mail). Email body includes a live count of remaining Founding-Brand spots (first 40 approved brands only) with the exclusive-badge FOMO copy so applicants share while they wait. Email failures logged, never blocking. (P2b) NEW `verify_image_magic_bytes(data, claimed_ct)` in `core.py` sniffs the first bytes of every image upload against the claimed MIME (JPEG=FFD8FF, PNG=89504E47…, GIF=GIF87a/89a, WEBP=RIFF+WEBP marker at offset 8). Applied to `/api/upload/image`, `/api/brands/upload-logo`, `/api/brands/upload-banner` — rejects HTML/JS/PE payloads that disguise themselves as PNGs (defeats stored-XSS via `/api/files/...` serving). (P1) **Component refactor** — split 4 monoliths into 12 subcomponents without any behaviour change: AddProduct 440→172 (-61%) via `product-form/{ProductPhotos,ProductDetailsFields,ProductSizes,ProductPricing}`; MyListings 625→448 (-28%) via `my-listings/{EditListingDialog,MoveToDeadStockDialog,RequestQuotaDialog}`; BrandDashboard 931→717 (-23%) via `brand-dashboard/{StripeConnectPanel,PayoutsWidget,ShareStorePanel}`; AdminDashboard 660→462 (-30%) via `admin-dashboard/{AdminApplicationsPanel,AdminDisputesPanel}`. Every subcomponent under 170 lines. Testing: **75/75 backend tests pass** (43 split_regression + 22 password_policy + 10 new iter13); **all 4 refactored pages render pixel-identical to before** with zero console errors.
- 2026-07: **Password security hardening** — (1) NEW SHARED POLICY in `core.py`: `MIN_PASSWORD_LENGTH=8`, `MAX_PASSWORD_BYTES=72` (matches bcrypt==4.1.3's silent-truncation limit — reject explicitly rather than accept a false-sense-of-security 100-char passphrase), `COMMON_PASSWORDS` blocklist (~50 top-breached: password, iloveyou, liverpool, arsenal, welcome, unveiledthreads, etc.), `validate_password()` raises 400 with specific actionable copy on: whitespace-only, <8 chars, >72 bytes UTF-8, or common. Deliberately NO composition rules (per UK NCSC guidance — they push users to "Password1!"). (2) APPLIED in `routes/auth.py`: `register()` runs `validate_password()` BEFORE the duplicate-email lookup (identical failure whether email exists or not — preserves anti-enumeration); `reset_password()` replaced its inline `len(...)<6` check with `validate_password()`. `UserCreate.password` Field constraints removed so `validate_password()` is the sole gate (returns 400 with actionable copy instead of generic Pydantic 422). (3) NEW `POST /api/auth/change-password` (rate limit 5/hour, authenticated): requires current_password (401 on mismatch), new must differ from current (400), runs `validate_password()`, deletes all outstanding password_reset_tokens for the user (matches reset flow's invalidation) — closes the gap where users had no way to rotate their password while the Resend domain remained unverified. (4) NEW frontend "Change password" card on `/account` (data-testid=`change-password-section`) — same brutalist black/silver/#39FF14 styling, surfaces backend's specific 400 detail (e.g. "That password is too common"). Register.js / ResetPassword.js client hint raised 6→8 with updated helper copy. (5) `server.py` startup env validation now includes `ENVIRONMENT` (previously omitted — if unset in prod, demo brand `demo@threadandbone.uk / Demo123!` seeds and the `Admin123!` admin fallback becomes live). LOGIN endpoint intentionally NOT validated — legacy users with pre-policy short passwords must still sign in. Full regression: 22/22 new tests (`tests/test_password_policy.py`) + 43/43 existing = 65/65 PASS.
- 2026-02: **Forgot-password / reset-password flow added**:
  - `POST /api/auth/forgot-password` (5/hour rate-limited) — accepts email, generates `secrets.token_urlsafe(48)` token, stores SHA-256 hash with 1-hour expiry in new `password_reset_tokens` collection. Always returns generic "If an account exists..." to prevent email enumeration. Sends branded HTML email via Resend with reset link.
  - `POST /api/auth/reset-password` (10/hour rate-limited) — consumes token + sets new bcrypt-hashed password, invalidates all other outstanding tokens for that user.
  - New pages: `/forgot-password` and `/reset-password?token=...`. "Forgot password?" link added to Login page. When register fails with "email already exists", the error block now shows "Sign in instead" + "Forgot password?" inline links.
  - Register error message softened from generic "Could not register account" to specific "An account with this email already exists" since we now offer a recovery path.
  - MongoDB TTL index on `password_reset_tokens.expires_at` auto-purges old tokens after 7 days.
  - **⚠️ Note**: Resend currently rejects sends to anyone other than `anthonygeorgiades2000@gmail.com` because no domain is verified. To go live, verify `unveiledthreads.co.uk` at https://resend.com/domains and update `SENDER_EMAIL` env var to e.g. `noreply@unveiledthreads.co.uk`. Until then password reset emails will fail to send for all users except the admin.
- 2026-02: **Production deployment fix** — `load_dotenv()` no longer overrides Kubernetes env vars + CORS now reads `CORS_ORIGINS` allowlist.
  1. **`load_dotenv()` no longer overrides Kubernetes env vars**. Previous `load_dotenv(override=True)` was forcing the local `.env` `MONGO_URL=mongodb://localhost:27017` over the production Atlas URL injected by Emergent → backend startup crashed with `ServerSelectionTimeoutError`. Now defaults to non-override behaviour: deployment env vars take precedence, `.env` only fills in vars the platform hasn't set.
  2. **CORS now respects `CORS_ORIGINS` env var properly**. Old code only read `FRONTEND_URL` and ignored `CORS_ORIGINS`. New logic: if `CORS_ORIGINS="*"`, uses `allow_origin_regex=".*"` (works with credentials), else exact origin allowlist from comma-separated env var.
  - **⚠️ ACTION REQUIRED IN EMERGENT DEPLOYMENT SETTINGS**: Ensure these env vars are set on the production deployment: `MONGO_URL` (Atlas), `DB_NAME`, `JWT_SECRET`, `STRIPE_API_KEY` (real live key), `STRIPE_WEBHOOK_SECRET`, `CORS_ORIGINS=https://unveiledthreads.co.uk`, `FRONTEND_URL=https://unveiledthreads.co.uk`, `EMERGENT_LLM_KEY`, `RESEND_API_KEY`, `SENDER_EMAIL`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `PLATFORM_FEE_PERCENT=4`.
  - **⚠️ Side effect on preview**: In preview, the pod injects a placeholder `STRIPE_API_KEY=sk_test_emergent` which now wins over our `.env`. To test real Stripe in preview, set `STRIPE_API_KEY` via Emergent preview env settings (not `.env`).
- 2026-02: Hybrid auto-approval with risk scoring + auto Stripe Connect onboarding.
  - New `calculate_application_risk()` computes a 0-100 score from: protected brand-name terms (Supreme/Nike/etc → +50), counterfeit keywords in description (wholesale/1:1/Yupoo/etc → +40), throwaway email domains (+30), account age < 24h (+15), short description / short or digit-heavy brand name (+10 each).
  - Score < 20 (`AUTO_APPROVE_RISK_THRESHOLD`) → instant approval. Otherwise queued for admin.
  - New `_finalise_approval()` helper shared by auto-approval + manual admin approval. Auto-creates Stripe Express Connect account on approval, generates `AccountLink` onboarding URL, and emails it to the new brand owner via Resend so they can finish KYC with one click (no need to log back in).
  - Admin pending queue now sorted by risk score DESC. Each application shows a colour-coded badge (green/yellow/red) + a list of triggered risk flags. "Auto-approved" badge marks applications that bypassed manual review.
  - Brand application success page now branches: "You're approved — check your email for the Stripe setup link" vs "We'll review within 24h" depending on outcome.
- 2026-02: **Tracking links**: Buyer + brand order pages now show clickable "Track on Royal Mail / Evri / DPD..." links for known UK couriers. Lib at `/app/frontend/src/lib/courierTracking.js`.
- 2026-02: **Security hardening**: Stripe webhook signature verification, slowapi rate limiting on auth + checkout endpoints, generic error message on register to prevent email enumeration.
- 2026-02: **Stripe Connect Direct Charges** (seller-liable), Shippo removed, boost paused.
- 2026-02: Stripe live test keys configured (sk_test_... backend, pk_test_... frontend). E2E verified: boost checkout + product purchase checkout both create real `cs_test_...` sessions against Stripe, status polling returns correct unpaid/paid state.
- 2026-02: Fixed `emergentintegrations.get_checkout_status` Pydantic validation bug by bypassing the library for status polls and using direct Stripe SDK (`stripe.checkout.Session.retrieve` + `session.metadata.to_dict()`). Helper `get_stripe_session_status()` defined at top of server.py.
- 2026-02: Switched `load_dotenv()` to `load_dotenv(override=True)` so `.env` always wins over pod-inherited env vars (pod was silently overriding STRIPE_API_KEY with `sk_test_emergent`).
- 2026-02: Verified Referrals "Coming Soon" redirect page renders correctly
- 2026-02: **GDPR Compliance Pass 1** — added `/privacy` Privacy Policy page (UK GDPR/DPA 2018 compliant), `/account` settings page with self-service Data Export (Art. 20) and Account Deletion (Art. 17), and a strictly-necessary cookies banner. Backend: `GET /api/account/export` returns full JSON dump (excluding `_id`/`password_hash`), `POST /api/account/delete` requires password + "DELETE" confirmation, cascades across all collections, anonymises orders for HMRC retention.
- 2026-06: **Founding spots FOMO copy** — new public `GET /api/brands/founding-spots` ({limit, taken, remaining}, registered before /brands/{id}); `components/FoundingSpots.js` exports `FoundingSpotsBanner` (on /apply: gradient-edged panel, badge, "Only X of 40 founding spots left", auto-hides at 0) and `FoundingSpotsCounter` (Brands page WANT TO JOIN section: "X/40 founding spots taken — apply while the badge lasts"; switches to "All 40 taken"). Live counts verified on both pages (3/40).
- 2026-06: **Founding Brand badge** — first 40 approved brands are permanently flagged `is_founding` (stamped in `_finalise_approval` via `db.brands.count_documents({}) < FOUNDING_BRAND_LIMIT` (env, default 40); existing 3 brands backfilled). New `FoundingBadge` component (components/FoundingBadge.js): metallic gradient-border pill with Gem icon + shimmer sweep (`.founding-badge` in App.css). Displayed on: Brands page cards (BrandCard), Brand profile header, ProductDetail "About the brand", BrandDashboard header. Flows automatically through existing brand-serialising endpoints. Verified via API + screenshots.
- 2026-06: **Referral programme gated (coming soon until funding)** — new `REFERRALS_ENABLED` flag in core.py (env, default false; backend/.env has REFERRALS_ENABLED="false"). While OFF: /api/referral/code, /apply, /share-links return 403 "coming soon"; /api/referral/credits returns zeros (so ProductDetail shows no credit line and checkout applies no credit); settle_paid_order grants no referrer credit (referral_uses.credit_pending stays pending for later); checkout credit auto-apply skipped. Frontend /referrals page already displayed "Coming Soon" — unchanged. To LAUNCH: set REFERRALS_ENABLED=true in prod env + backend/.env and restart; pending credit_pending flags will start crediting on future first purchases. Tests updated: iter11 referral test skips while gated; split_regression referral_code accepts 200/403. Verified: 403s + zero credits via curl, Coming Soon page + no credit line via screenshot, 43/43 + 5 pass/1 skip regression.
- 2026-06: **4 security/correctness fixes (user-specified)** — (1) INPUT VALIDATION: ProductCreate fully constrained (price >0 ≤10000, stock 0-100000, name 2-120, desc 10-3000, sizes/images non-empty, gender/condition whitelisted via field_validator); new ProductUpdate (all-Optional, exclude_unset) replaces raw request.json() in PUT /products/{id}; ReferralApplyRequest (code 3-40) + BrandProfileUpdate models replace remaining raw-json endpoints. (2) OVERSELL GUARD: settle_paid_order decrements stock conditionally (stock>0); when oversold: order flagged oversold=True, OVERSOLD logger.error, brand 'oversold_order' notification, settlement still completes; /api/admin/stats now returns oversold_orders count. (3) PASSWORD RULES: UserCreate password 8-128 + name 1-100; reset-password enforces 8-128 with message 'Password must be 8–128 characters'. (4) EMAIL HTML ESCAPING: core.esc() (html.escape) applied to ALL user-sourced strings in email HTML — create_notification title/message, receipt/shipped/delivered/abandoned/digest emails (product/brand names, size, courier, tracking), forgot-password email. Files: core.py, routes/{products,orders,disputes,auth,brands,referrals,shipping,engagement}.py. Verified by testing agent: 23 new tests (tests/test_fixes_iter12.py) + 49 regression = 72/72 PASS (report iteration_12.json). Test suites note: run login-heavy suites with a gap to avoid the 10/min auth rate limit.
- 2026-06: **Code review (production) — 5 functional defects fixed** — (1) REFUNDS: dispute refunds previously never executed (no charge_id ever stored; wrong reverse_transfer for direct charges) — now `settle_paid_order` stores `payment_intent_id`, refunds are created ON the connected account with `refund_application_fee=True`, legacy orders recover the PI from the session, and on Stripe failure the dispute STAYS OPEN (502) instead of falsely telling buyers they were refunded. (2) GDPR DELETE: cascade fixed — `str(brand_doc["_id"])`, `db.wishlist` (was wishlists), conversations via participant_1/participant_2 (was nonexistent 'participants'); export conversations query fixed too. (3) SCHEDULERS: abandoned_checkout_loop was never scheduled — startup now runs 3 loops (low-stock digest, abandoned checkout, order reconciliation). (4) REFERRALS: £5 referrer credit now granted atomically on referred buyer's first paid order; credits auto-apply against the Buyer Protection fee at checkout (fee line + application_fee_amount reduced, `credit_applied` stored on order, consumed at settlement); ProductDetail shows referral-credit-line, receipts (UI + email) show credit row. (5) SETTLEMENT: new idempotent `settle_paid_order()` (atomic stock_deducted claim — fixes double-deduct race) + `order_reconciliation_loop` sweeps paid-but-unreturned orders every 10 min + admin trigger POST /api/admin/orders/reconcile. Also: admin dispute summary now reads total_charged. Verified by testing agent 49/49 (43 regression + 6 fix tests, /app/backend/tests/test_fixes_iter11.py). NOTE: demo@threadandbone.uk retains £5 referral credit in dev DB (powers the credit UI demo).
- 2026-06: **Backend restructure (monolith split)** — server.py (~5,000 lines) split VERBATIM into: `core.py` (env/config, db, models, auth deps get_current_user/require_brand/require_admin, limiter, scan_message moderation, risk scoring + _finalise_approval, slug utils, fee calc, object storage helpers, create_notification) + 18 modules in `routes/` (auth, account, applications, brands, products, payments[boost+webhook], connect, disputes[+admin stats], uploads, orders, engagement[digests+abandoned+notifications], reviews, shipping, wishlist, analytics, referrals, messaging, community) + slim `server.py` (imports route modules in original registration order, then seed/startup/middleware/include_router — entrypoint stays `server:app`). All modules share the single `api_router` from core via `from core import *` plus explicit imports for underscore helpers (star import excludes them — this caused the only bugs during the split, all fixed: _finalise_approval, _seller_cap_reached, _seller_slots_used, _normalise_slug_candidate, _brand_dead_stock_count, _brand_quota, _rate_limit_exceeded_handler). 100 API routes registered, identical to monolith. Monolith backup: /app/memory/server_monolith_backup.py.bak. FULL REGRESSION: 43/43 backend tests across all 18 modules + 5/5 frontend smoke (report iteration_10.json; reusable suite at /app/backend/tests/test_split_regression.py, ~6s runtime — re-run after any route change).
- 2026-06: **Code review pass** — (1) Backend: replaced `__import__("json")` and inline `import json as _json` with top-level `import json`. (2) Fixed pre-existing `/api/account/export` crash: `brand_doc["id"]` KeyError → `str(brand_doc["_id"])`; also fixed `c.get("id")` → `str(c["_id"])` for conversations and wrong collection `db.wishlists` → `db.wishlist`. (3) Frontend: wrapped all data-fetchers in `useCallback` with correct dependency arrays across 11 pages (Products, ProductDetail, OrderSuccess, MyOrders, MyListings, Messages, Community, DeadStock, AdminDashboard, BoostSuccess, BrandAnalytics) — webpack now compiles with ZERO lint warnings. Products.js uses a `searchQueryRef` so debounced filter-apply behaviour is unchanged. NOT changed (false positives): `is None`/`is True`/`is False` comparisons (correct tri-state idiom), skeleton-array index keys (static lists), intentional empty catches for logged-out 401s, CookieBanner localStorage (stores only a non-sensitive consent flag). DEFERRED (per longstanding decision): splitting server.py (~5000 lines) and the large page components. Full regression by testing agent: 12/12 frontend flows, no infinite fetch loops (report: /app/test_reports/iteration_9.json).
- 2026-06: **Abandoned checkout win-back** — `send_abandoned_checkout_emails()` emails buyers whose orders sat unpaid (status initiated/unpaid/expired) for 1-48h: branded "You left something behind" email with price, scarcity line when stock ≤5, and COMPLETE YOUR ORDER button → product page. Skips buyers who later paid for the same product and sold-out/deleted products (flag set so they're never emailed). Once per order (`abandoned_email_sent`), runs via startup `abandoned_checkout_loop()` every 30 min; admin trigger `POST /api/admin/abandoned-checkout/run`. Env tunables: `ABANDONED_MIN_AGE_MINUTES=60`, `ABANDONED_MAX_AGE_HOURS=48`. Verified sent + duplicate-guarded in dev.
- 2026-08: **CSV product import (Iter 14)** — new `/api/products/import/csv` (brand-auth, 5MB / 200-product cap, 5/min rate-limit) accepts Shopify/Etsy/WooCommerce/Squarespace/generic CSVs. Column-alias table maps Title/Body(HTML)/Variant Price/Product Type/Option1 Value/Variant Inventory Qty/Image Src etc. Groups multi-row-per-variant exports by Shopify Handle (or repeated title) into a single product with sizes array + distinct images. External image URLs are downloaded, magic-byte verified, stored in our own object storage (`/api/files/unveiled-threads/imports/<brand>/...`) — external CDN links are NEVER persisted on the product. Products land as `status="draft", import_source="csv"`; GET /api/products list and GET /api/products/{id} hide drafts from non-owners; checkout rejects drafts (404). New endpoints: `GET /api/products/my/drafts`, `POST /api/products/{id}/publish` (re-runs full validation gate), `POST /api/products/drafts/publish-all` (bulk, returns skipped_details). Row-level errors do not abort the whole file — they're returned in the `errors[]` list with row# + reason. Frontend page at `/brand/import` with dark/neon-green theme: dropzone + supported-columns panel + import summary card + drafts grid (per-card blocker list, individual Publish, Publish-all-valid bulk button, Edit via existing dialog, Delete). Backward-compat: products with no `status` field are treated as published. Verified 16/16 backend pytest cases + Playwright E2E.
- 2026-06: **Delivered email** — `send_order_delivered_email()` sends buyers a branded "your order has arrived — leave a review" email (LEAVE A REVIEW button → /orders) when a brand marks status `delivered` via `PUT /api/orders/{id}/shipping-status`. Once per order (`delivered_email_sent` flag, duplicate-guard verified); generic notification email suppressed for delivered status. Domain verification for Resend is a USER action (dashboard + DNS) — the API key is send-only restricted so it can't be checked programmatically. Once verified, set `SENDER_EMAIL=noreply@unveiledthreads.co.uk` in prod env + backend/.env.
- 2026-06: **Shipped email** — `send_order_shipped_email()` sends buyers a branded Resend email with tracking number + clickable "TRACK YOUR PARCEL" button when a brand marks an order shipped (`PUT /api/orders/{id}/ship`). Backend `COURIER_TRACKING_URLS` mirrors `frontend/src/lib/courierTracking.js` (Royal Mail / Evri / DPD / Yodel / UPS / FedEx). `create_notification()` gained a `send_email` flag — ship flow passes `send_email=False` so the buyer gets ONE branded email, not a duplicate generic one. Verified delivered to owner's gmail. Demo order is now in "shipped" state with tracking AB123456789GB.
- 2026-06: **Email receipts + payout history + low-stock digest** — (1) `send_order_receipt_email()` sends buyers a branded Resend receipt (Item / Buyer Protection / Shipping / Total) when an order settles as paid in `get_order_status`, guarded by `receipt_email_sent` flag. (2) `/api/connect/payouts` now returns a `history` list (last 20 payouts); BrandDashboard payouts widget renders a scrollable Payout History with status badges. (3) Weekly low-stock digest: `send_low_stock_digests()` (best sellers with stock ≤ LOW_STOCK_THRESHOLD and ≥1 sale in 30d, max 5 items, per-brand 7-day guard via `brands.low_stock_digest_sent_at`), runs from a startup `low_stock_digest_loop()` every 6h; admin manual trigger `POST /api/admin/low-stock-digest/run` returns a send report. Both email types verified delivered to the owner's gmail in dev.
- 2026-06: **Fee line everywhere + Payouts widget** — `GET /api/orders/status/{session_id}` now returns an `order` receipt object (price / platform_fee / shipping / total); OrderSuccess page shows a full receipt card; MyOrders expanded view shows a Receipt breakdown with a "Buyer Protection" line. New `GET /api/connect/payouts` (Stripe Balance + Account schedule + Payout list on the connected account) powers a Payouts widget on BrandDashboard (Available / Pending / Next payout / Last payout); returns `{connected:false}` gracefully and the widget hides when the brand has no Stripe account. A seeded paid demo order (`session_id=cs_test_receipt_demo_123`, buyer demo@threadandbone.uk, £65 + £3.74 BP + £4.99 ship = £73.73) remains in the dev DB for receipt testing.
- 2026-06: **Buyer fee restructure** — flat 4% replaced by "Buyer Protection" fee: `min(subtotal*0.05 + 0.49, 6.00)` paid by buyer, once per order, on item subtotal only (not shipping). Sellers unchanged (receive 100% of price + shipping). `calculate_buyer_fee()` in server.py; frontend shared config `/app/frontend/src/lib/fees.js`. Stripe checkout now shows 3 line items (product / Buyer Protection / shipping); `application_fee_amount` uses new fee in pence. Copy updated: ProductDetail (with info tooltip), AddProduct "Buyer sees" preview, BrandDashboard, Terms §6.1/6.2/liability cap, BuyerProtection page. Note: orders are single-item, so "once per order" is trivially true; if a multi-item basket is ever added, fee must be computed once on combined subtotal.
- Earlier: Instagram & website removed from brand profiles; admin credentials updated; T&Cs; community + advanced filters

## Roadmap
### P1 (next)
- (none queued — user dropped product colour variants)

### P2 (backlog)
- Push notifications (currently short-polling)
- Refactor `server.py` into modular routes (it's ~3500 lines)
- **GDPR Hardening Pass 2**: Admin 2FA (TOTP), field-level encryption for shipping addresses at rest, log redaction middleware, ICO registration reminder in admin panel, DPA template links for Stripe/Resend/Atlas.

## Credentials
See `/app/memory/test_credentials.md`.

## Critical Rules
- DO NOT re-add Instagram / website links to brand profiles (anti off-app sales policy)
- DO NOT bypass / weaken content moderation in messages & comments
- Buyer fee is "Buyer Protection": min(subtotal×0.05 + £0.49, £6.00), buyer-paid, sellers keep 100% of price + shipping (env overrides: PLATFORM_FEE_RATE / PLATFORM_FEE_FIXED / PLATFORM_FEE_CAP)
- User language: English (UK spelling)
errides: PLATFORM_FEE_RATE / PLATFORM_FEE_FIXED / PLATFORM_FEE_CAP)
- User language: English (UK spelling)
